# Local SQLite database for lead capture
DATABASE_URL="file:./prisma/dev.db"

# Optional: where to send a notification when a new teardown is requested
# (left unused by default — leads are persisted to the database)
# NOTIFY_EMAIL="nathan@maubourg.studio"
